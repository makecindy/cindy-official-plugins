# My Browser

[简体中文](README.zh-CN.md)

A local candidate plugin connecting Cindy to your everyday signed-in browser. Version **0.3.x** adds content-first reading, browser/profile-bound routing, a minimal Art-generated icon and browser-specific setup. It is **not yet a publicly distributed consumer release**.

## Reading vs interaction

- Public page: use the host's ordinary public reader; no extension required.
- Signed-in page: `browser_read` directly extracts bounded DOM content. Default `content` returns main text and links; `extract` returns records. Readiness is awaited inside the extension, not through repeated model calls.
- X replies/mentions: `browser_read({recipe:"x_mentions",limit:5,maxChars:4000})` combines destination, waiting, text, author, time and links in one call. No home/menu snapshots, private API tokens or UI clicks. This is the rendered slice, not all history or necessarily unread-only.
- Interaction: request a scoped `snapshot` for fresh refs, then `browser_act` with site authorization. Reading does not require an interaction grant.
- Multiple connected profiles: pass the `browser` connection id; ambiguity returns `BROWSER_REQUIRED`, never a guessed account. Tab listing supports `host` and `limit`.
- `after` returns rows before a previously seen permalink in newest-first lists. `cursorFound:false` and `truncated:true` prohibit completeness claims. No automatic scrolling.

The on-demand workflow manual is `manual/browser/MANUAL.md`. Parameter names and enums remain stable across locales.

## Browser support and installation readiness

| Browser | Code/artifact | Verification and release status |
| --- | --- | --- |
| Chrome | Shared MV3 extension, Chrome 120+ | Real Chromium integration; everyday Chrome 0.2.0 reads verified. 0.3.0 real Cindy/everyday Chrome X extraction also verified. |
| Edge | Shared Chromium extension, separate store artifact and profile routing | Protocol and Windows launcher tests; no real Windows/Edge device verification yet. |
| Safari | WebExtension adapter + generated macOS container app | Universal arm64/x86_64 Release build and identity-pairing tests; no signed consumer installation or real Safari extension execution yet. |

Settings makes ZIP installation the primary Chrome/Edge route: open the extensions manager and ZIP location, enable Developer mode, then drag the ZIP onto the page. No unzip or directory selection is needed. Connection checks are automatic; opening a page is not installation success. Directory loading remains a fallback. Manual ZIP dropping in official Chrome still needs real-device verification and may be blocked by managed browser policy.

Safari still requires a store release or signed/notarized app, neither configured yet. Chrome/Edge ZIP installation does not depend on store publication. Unknown extension identities still require real Cindy pairing confirmation. After updates, drag the new ZIP again; directory installations use Reload. Keep only one copy.

Installation cards appear only for connected browsers or installed browsers with a deliverable ZIP/store route. Unpublished Safari and browsers absent from this device are hidden, with no coming-soon placeholder.

## Security and limitations

- New installations allow reading and interaction on all public websites. Saved restrictions are preserved until explicitly removed in settings: old policies lack provenance, so matching the legacy preset does not prove user intent; the all-sites switch enables broad interaction, while read blocks and interaction exclusions still take precedence. Domain grants include subdomains; `=host.example.test` is exact-only. New grants/unblocking require real confirmation; cancellation changes nothing.
- Only `127.0.0.1:18810–18819` is bound. HTTP cannot enqueue work or modify policy. Host, extension origin, profile identity and current bridge session are checked; no CORS. Unknown extension identities get no session/jobs before approval. This is browser-origin protection, not a defense against malicious same-user native software or already-trusted extensions.
- Jobs and acknowledgments/results are bound to one profile. Delivered actions are never replayed. `execution:executed` proves DOM dispatch, not successful sending/purchase; `unknown` requires checking before repetition.
- No cookie export, arbitrary JS tool, debugger or credential extraction. Hidden/password/payment-code fields are excluded, but arbitrary page text may still contain private information. AI may receive this data off-device. Visits/reads contact websites and may affect server-side notification read state.
- Public-looking hostnames are insufficient: before reading or acting, the extension requires a public peer IP reported by `webRequest` for the current main document, correlates navigation/request timestamps, and targets that exact `documentId`. Private/missing addresses, unobserved older tabs and ambiguous navigation evidence fail closed with `ADDRESS_UNVERIFIED`; tab metadata is redacted. No automatic refresh or replacement tab is attempted. A manually refreshed public page can supply new evidence. Cache/proxy/browser configurations that cannot provide public peer evidence are unsupported for that page. This prevents extraction from private main documents; it is not a network firewall: navigation may already have contacted the destination, and arbitrary page content can originate elsewhere. `webRequest` and `webNavigation` observe main-document address/identity only; no cookie/header capture.
- At most three background tabs per profile, reclaimed after ten idle minutes. User-owned/focused/navigated tabs are not reclaimed. Safari without session storage keeps the ownership ledger in memory rather than persisting tab ids across browser restarts.
- Refs are isolated-world Element maps, not page-writable attributes, and expire on navigation/new snapshots. Synthetic keys/hover may be ignored. No cross-origin frames or closed Shadow DOM. Form submission uses one mechanism only.
- Text/record values default to a 6000-character budget; JSON/link overhead is additional. Tabs default to 20, max 100, with an additional aggregate budget. Overlong tab URLs are not exported. A timeout/empty shell is not proof of no results.

## Build and verification

First-party JS and Node built-ins only. No runtime dependency installation. `node/worker.cjs` activates unconditionally because Cindy requires the entry rather than running it as `require.main`. Runtime subprocesses are restricted to fixed browser/app launchers and Safari signature checks.

```sh
python3 scripts/package-my-browser-zip.py
node scripts/validate-plugin-manifest.mjs ./my-browser
node --test .tests/my-browser.test.mjs .tests/my-browser-multibrowser.test.mjs .tests/my-browser-tabs.test.mjs .tests/my-browser-orchestrator.test.mjs
PLAYWRIGHT_CORE=/absolute/path/to/playwright-core node --test .tests/my-browser.browser.test.mjs
# macOS build host with Xcode; fresh output directory (existing builds are never overwritten)
node scripts/build-my-browser.mjs /absolute/output/directory all
```

The builder creates Chrome/Edge store ZIPs and a Safari Xcode project/universal app. Without signing credentials, it clearly labels the Safari artifact **not consumer-ready**. Optional `MY_BROWSER_SIGN_IDENTITY` and `MY_BROWSER_DEVELOPMENT_TEAM` enable signing; `MY_BROWSER_NOTARY_PROFILE` names an existing Apple notarytool keychain profile for notarization. No credential values belong in source, plugin settings or artifacts. Only after successful notarization/assessment copy the output `native/` into the plugin for packaging. Signing/notarization paths are not verified on this machine because it has zero available signing identities.

2026-09-15 local evidence:
- Manifest, 58 Node/HTTP/localization/provisioning/workflow tests plus the isolated Chromium integration suite passed (59 total).
- Real Chromium suite covers read/extract/interaction, waiting for hydration, one-call X-shaped fixture extraction, cursor bounds, stale refs, denial/revocation, redirects, tab cleanup, popup, pairing cancellation/persistence, settings failures and 330px layout.
- X-shaped **fixture**, not a real X latency claim: 5 records in 1 read call, 714ms, 792 bytes of result JSON in the recorded run.
- Safari universal Release build succeeded. No Safari signature, notarization, App Store release or real Safari runtime claim.
- Installed Cindy is 0.1.82; minimum remains 0.1.64 (Manifest v3). Manual support predates that minimum (first containing stable tag v0.1.48).
- Previous 0.2.0 real Cindy/everyday Chrome checks passed for example.com and the user's X notifications; interactions remained denied. 0.3.0 was subsequently installed in Cindy and reloaded in everyday Chrome: the real signed-in X mentions page returned 5 structured replies in one call (bridge total 4329ms, including 1785ms readiness wait). A second incremental read found its anchor and returned zero preceding rendered rows in 22ms; this is not a fresh-network or all-notifications completeness claim. The installed on-demand manual was also read successfully. Existing site permissions were preserved.

0.3.1 fixes repeated tab creation after redirected load timeouts, lost aliases after focus, and eviction that discarded bookkeeping without closing the actual tab. Protected tabs stay counted; unexpected navigation and exhausted capacity stop with non-retryable errors. Five regression tests plus the real Chromium redirect-retry test cover these paths.

0.3.8 closes a permission-ordering race found in review: `sync()` read the stored policy and applied it to the worker in two separate steps, so a settings save landing between them could be overwritten by the stale snapshot, leaving a site the user had just revoked actionable until the next sync. Reading and applying now share one critical section with `save()`. Two regression tests exercise the real `main.js` orchestrator with stubbed host APIs: one stalls a sync between the read and the apply, lets the save overtake it, and asserts the revocation both survives in storage and stays denied for later tool calls; the other checks concurrent saves and tool calls neither fail nor deadlock. Both fail against the pre-fix `sync()`.

0.3.9 covers the two remaining review findings on the declared contract. A transport failure while reading (timeout or broken stdio) used to escape `node()` and land in the generic handler, which only knew about interactions, so `browser_read` claimed `not_executed` even though the worker may already have accepted the job and visited the page. `node()` now converts a rejected host request into the same structured `NODE_UNAVAILABLE` as a returned worker error, so reads report `unknown`. A third orchestrator test throws from the stubbed host request and asserts `unknown`; it fails against the pre-fix `node()`. Second, the agent-facing `description`/`whenToUse` and all four locales no longer claim Safari as a connectable browser: this package ships no signed Safari app and no store entry, the settings page already hides the Safari card, so an agent following the old text would have driven users into an install that cannot complete. Safari implementation, pairing, the build script and the README/Manual notes are unchanged; only the overstatement is removed, and the Manual now says explicitly that Safari cannot be installed from this package.

0.3.10 closes two credential-exposure findings. Tab URLs used to be returned verbatim, so a tab sitting on an OAuth callback, password reset or magic link handed `?code=…` or `#access_token=…` straight to the model; the public-address check and host filter did not remove them. A shared `redactUrl` now masks credential-named query parameters and long opaque token values, masks a fragment only when it carries `key=value` data (a plain route fragment such as `#/home` is kept so tabs stay identifiable), and is applied both when the extension builds a tab row and as the bridge's last gate before any result reaches the model — including read and navigation results, not just tab lists. Second, sensitive-field detection matched `autocomplete` exactly, so real checkout and OTP markup (`autocomplete="section-checkout billing cc-number"`) slipped through and stayed visible as refs and typable; detection now token-matches the standard keyword list (`cc-number`, `cc-csc`, `cc-exp*`, `one-time-code`, `current-password`, `new-password`, `type=password`, `type=hidden`). Both tool contracts and all four locales were updated with the new behaviour. Regression coverage: a policy unit test for `redactUrl` (masked code/state/token/fragment, preserved `q=cats` and `#/home`, unchanged unparsable input) and two real-Chromium checks — the fixture now carries multi-token card/OTP fields that must be refused and absent from snapshots, and a callback tab with `code`/`state`/fragment tokens must list with the credentials removed while still being listed. Both new Chromium checks fail against the pre-fix build (`an OAuth code must never reach the model`).

0.3.11 completes that sanitizer. The first exit gate only covered the page URL and tab rows, but content reads also return page links, so a link containing a reset token or magic link still reached the model. The gate now covers every URL-bearing shape — page URL, tab rows, `links[].url`, and any extracted field value that is itself an http(s) URL (field names are caller-chosen, so they cannot be matched by key). Test placeholders in the fixture and tests were also switched to obvious fake UUIDs so no credential-shaped literal remains in the repository. Regression coverage: the fixture carries a `/reset?token=…` link, and a content read must still list it while the token is absent and ordinary links stay usable; removing just the links branch reproduces the failure (`a magic-link token in a page link must never reach the model`).

0.3.12 widens both boundaries. Credentials in password-reset and magic links usually sit in a path segment (`/reset/<token>`, `#/verify/<token>`), which the query/fragment sanitizer never inspected, so those URLs still reached the model while the contract claimed magic links were redacted. Segmentation now masks a token-like segment (>=16 chars, punctuation-free, contains a digit) only when it follows a credential-context word such as reset/verify/invite/auth, which keeps ordinary deep paths intact: `/commit/<sha>`, `/user/12345`, `/reset-password/success` and `/notifications/mentions` are unchanged in the unit test. Separately, field detection relied on standard `autocomplete`, so `<input name="card-number">` or `<input name="otp">` with no `autocomplete` stayed visible as refs and typable. Detection is now conservative: `type`, multi-token `autocomplete`, and the element's own naming (name/id/placeholder/aria-label/data-testid, camelCase- and separator-aware) all feed one predicate used by snapshot, extract and act. The naming rule is scoped to form-like elements, so a button or link that merely mentions a card stays visible and actionable (covered by an assertion). Fixture and assertions cover both: a `/reset/<fake-uuid>` link must list with the token absent, and unlabeled-by-autocomplete `otp`/`card-number` fields must return `SENSITIVE_FIELD` and stay out of snapshots. Reverting only the naming clause reproduces `undefined` instead of `SENSITIVE_FIELD`; reverting only the path rule reproduces the path-segment assertion failure.

0.3.13 clears URL userinfo. A page-supplied link or extracted `href` can be `https://user:password@example.test/`; task URLs are rejected by `check`, but page content never passes through it, and `redactUrl` only rewrote query, fragment and path, so Basic Auth credentials in a link reached the model. The sanitizer now strips `username`/`password` (verified to yield `https://example.test/private`), and the tool contract lists userinfo alongside the other redacted forms. Coverage: a unit assertion on the parsed result plus a fixture link carrying userinfo, asserted absent from a content read. Both fail against the pre-fix sanitizer.

0.3.14 closes a percent-encoding bypass and bounds the payload. A reset/magic-link token containing an encoded character (`/reset/Abc%2F1234567890`) kept its `%2F` in `pathname`, so the segment charset test judged it a normal path word and returned the whole credential; segments are now decoded before the token test (an all-digit token still matches, so decoding does not require a letter). Separately, `document.title` and every `links[].url` were unbounded: the fixture page now returns a 600000-character title and a 120000-character href, which exceeds the bridge's 512000-byte request limit, so the extension discarded the result and the caller waited out a 45-second `BROWSER_TIMEOUT`. Titles are capped at 300 characters, link URLs at 2048 with a 20000-character link budget, and the response sets `truncated` when a title, link or text is cut. Coverage: unit assertions for the encoded segment in both path and fragment, and a real-Chromium read of the oversized page that must return `ok` with a bounded title and `truncated:true`. Reverting only the decode yields the encoded-token assertion failure; reverting only the bounds reproduces `BROWSER_TIMEOUT` after 45s.

0.3.15 makes sensitivity follow real editability. The naming rule was gated on the literal selector `[contenteditable=true]`, but `contenteditable=""` and `contenteditable="plaintext-only"` are editable too, so a `<div contenteditable role="textbox" name="otp">` was judged a normal field, appeared in snapshots through `role=textbox`, and was accepted by the `type` branch (which already used `el.isContentEditable`). The predicate now uses `el.matches('input,textarea,select') || el.isContentEditable`, so the sensitivity check and the action branch agree, and the snapshot selector enumerates `[contenteditable]` rather than only the literal `true`. Coverage: the fixture carries an empty-attribute OTP region and a `plaintext-only` card region, both of which must return `SENSITIVE_FIELD` and stay out of snapshots; restoring the literal selector reproduces `undefined` instead of `SENSITIVE_FIELD`.

0.3.16 fixes the order of redaction and truncation, and replaces the encoding-chasing heuristics with one normalized rule. Links used to be cut to 2048 characters in the injected function before the bridge redacted them, so a page could place a credential across the cut and leak its prefix; the transfer cap is now applied to the redacted string, in the service worker where the policy module is available. The injected function never cuts a URL at all: a link longer than 8192 characters or beyond a 200000-character page budget is dropped whole, which bounds the payload without ever splitting a credential. On the sanitizer side, a percent-encoded context word, a double-encoded token and an encoded `=` in a fragment each used to evade a single-pass, charset-based check; every judgement now runs on a bounded, repeatedly decoded form (up to four passes) and a credential is identified by length alone after a context word, because a charset heuristic is exactly what repeated encoding kept defeating. Coverage: a fixture link places a credential so that a 2048-character cut would retain its first 12 characters, asserted absent; unit assertions cover an encoded token, an encoded context word, a double-encoded token and an encoded fragment key/value. Truncating before redaction reproduces `truncation must not expose a credential prefix`; disabling normalization fails the encoded-token assertions. Test placeholders are fake UUIDs only, as the fixture rule requires.

0.3.17 stops text dumps from returning a value held in a region that is already treated as sensitive. `password`, `autocomplete` and naming detection excluded such elements from snapshot refs and from `extract`, but `text`, `content` and the snapshot text field serialized `root.innerText` unfiltered, so an OTP or card value inside a `contenteditable` region was returned even though the field itself was hidden. Text is now read from a clone with the sensitive descendants removed; the clone is only built when the region actually contains one, and ordinary text is unchanged. Coverage: the fixture's named OTP region now carries a value, asserted absent from `content`, `text` and snapshot text while ordinary page text still appears. Restoring the unfiltered read reproduces `sensitive contenteditable text must not be returned`.

0.3.18 closes two gaps in that sanitizer. The transfer cap shortened a link without marking it, so a URL between 2049 and 8192 characters came back silently cut while the contract promised `truncated:true` marks cuts; the flag is now set whenever the cap actually shortens the redacted URL. And the text scrubber only looked at descendants, so targeting the sensitive region itself with `selector` returned its unfiltered `innerText`; the root is now checked too and a sensitive root yields no text. Coverage: a `long.test` page whose single link has a 2500-character path must report `truncated:true` with every returned URL within the cap, and reading `#otp-region` directly must return no credential. Removing the flag reproduces `a capped link URL must set truncated`; removing the root check reproduces `targeting a sensitive region directly must return no credential`.

0.3.19 fixes two more output boundaries. The sensitive-region scrubber read text from a detached clone, and a detached `innerText` falls back to textContent, so `display:none` or script content leaked into the text whenever the page also had a sensitive field; the subtraction now happens on the live tree by briefly detaching the sensitive subtrees, reading rendered text, and restoring them synchronously in reverse order, so rendered semantics are preserved and page scripts never observe the gap. Separately, the snapshot text cap cut at 6000 characters without setting `truncated`, so an agent could read a clipped body as complete; the flag is now set when that cap applies. Coverage: the fixture carries a `display:none` marker that must stay out of the text, and a `long.test` snapshot with a 7000-character body must report `truncated:true`. Restoring the clone-based read reproduces `hidden content must not be returned when a sensitive region is filtered`; removing the flag reproduces `a capped snapshot text must set truncated`.

0.3.20 stops reading from mutating the page. Briefly detaching the sensitive subtrees fixed the hidden-content leak but still touched the live DOM: a custom element fired `disconnectedCallback`/`connectedCallback` and a page `MutationObserver` recorded the change, so a "read" was observable as a write. Text is now extracted with a `TreeWalker` over the live tree that skips non-rendered subtrees (`display:none`, `visibility:hidden`, `hidden`, script/style/template) and sensitive elements, walking at most the requested length; nothing is inserted, removed or reordered. Coverage: the fixture defines a custom element with lifecycle counters plus a `MutationObserver`, and a text read must return the sensitive value neither and leave both counters at zero. Restoring the detach-based implementation reproduces `a read must not fire custom element lifecycle callbacks`.

0.3.21 moves `extract` onto the same rendered-text path. Attribute-less extract fields fell back to `textContent` when `innerText` was empty, so a visible wrapper whose children are all `display:none` or script returned hidden data such as a CSRF value; fields now read through the same TreeWalker-based extraction as `content` and `text`, and hidden descendants are skipped there too. Coverage: a fixture wrapper containing only a hidden span must yield nothing through extract, while an ordinary element still returns its rendered text unchanged. Restoring the `textContent` fallback reproduces `extract must not return hidden descendants`.

0.3.22 restores rendered-text fidelity in the filtered path. The first TreeWalker collapsed every text node separately and joined with a space, so `<span>A</span><span>B</span>` became `A B` and `<br>`/block breaks disappeared; it also rejected a `visibility:hidden` subtree outright, dropping descendants that explicitly restore `visibility:visible`. A region with no sensitive descendant now returns `innerText` untouched (exact), and only a region that actually contains one is walked: adjacent inline runs join with nothing, a differing nearest block ancestor or `<br>` emits a line break, `white-space: pre` keeps its whitespace, and visibility is judged per text node so a re-shown descendant survives while hidden text stays excluded. Coverage: fixture probes for `AB`, block breaks, `<br>`, and a re-shown span; the previous implementation reproduces `adjacent inline runs must not gain a space`.

0.3.23 also rejects a non-rendered root. A `TreeWalker` never runs its filter on the root it is given, so a `selector` that matched a `display:none` wrapper (or an element under a hidden ancestor) fell through to `innerText`, which on a non-rendered node degrades to `textContent` and returned hidden values. The root is now checked with `checkVisibility()` (display:none on itself or an ancestor) before any text is produced; a `visibility:hidden` root is deliberately still handled by `innerText`, because that keeps descendants which restore `visibility:visible`. Coverage: a `display:none` wrapper probed through both `text` and `extract` must return nothing; removing the check reproduces `a display:none root must not return its text`.

0.3.24 fixes two fidelity regressions in the filtered path. A whitespace-only text node was skipped outright, so `<span>Signed</span> <span>in</span>` came back as `Signedin`; such a run is now kept as a separator between inline content and cleaned up at block boundaries, so `Signed in` survives while block breaks stay tidy. And the root check used `checkVisibility()`, which reports false for a `display:contents` wrapper even though its children render; non-rendered is now computed from `display:none` on the element or an ancestor, so a `display:contents` wrapper is read normally. Coverage: fixture probes for `Signed in` and for a `display:contents` wrapper read through `text`. Restoring the whitespace skip reproduces `a visible inline space must be preserved`; restoring the `checkVisibility()` check reproduces `a display:contents wrapper must still be readable`.

0.3.25 replaces the per-property visibility chasing with one conservative predicate. `content-visibility:hidden` keeps `display:block` and `visibility:visible` while not rendering its contents, so a hidden verification code or private text inside such a subtree was still returned. There is now a single `notRendered` rule — the element or an ancestor has `display:none`, or `content-visibility` is `hidden` (or `auto` and currently skipped) — used for the root, while the walker only needs each element's own state because a suppressed ancestor is already rejected. `display:contents` stays readable, since such a wrapper has no box of its own but its children render. Coverage: a `content-visibility:hidden` fixture probe must stay out of the text; removing the check reproduces `content-visibility:hidden content must stay excluded`.

0.3.26 removes the Chrome-version dependency from that rule. The `auto` case used `checkVisibility({contentVisibilityAuto:true})`, an option a Chrome 120 engine ignores while the extension still declares `minimum_chrome_version: 120`, so skipped off-screen content could still be returned there. It now asks the browser's own rendering-aware `innerText`: an `auto` element that has text content but renders none of it is skipped. That uses no version-dependent option, needs no minimum-version bump, and is evaluated only for `auto`/`hidden` elements so the cost stays bounded. Coverage: a `cv.test` page carries a password field so the filtered path runs, with an on-screen `content-visibility:auto` element that must stay readable and a below-the-fold one that must stay excluded; removing the `auto` rule reproduces `engine-skipped content-visibility:auto content must stay excluded`.

0.3.27 applies the redact-then-cap rule to the last two boundaries. `extract` cut every value to the remaining `maxChars` before the bridge redacted it, so when the budget left only a short prefix of a reset token the path redactor no longer recognised it; URL attribute values (`href`/`src`) now travel whole and are only dropped when longer than the hard cap, so redaction always sees the complete string, while text values keep the budget cut. And a content read that stopped at the requested link count returned `truncated:false`, so a caller could not tell "the page had these links" from "the list was cut"; the count branch now sets the flag. Coverage: an extract of a long-labelled link whose token would previously be cut short, and a `links.test` page read at `limit:2` (must report truncation) versus `limit:10` (must not). Removing the URL branch reproduces `a credential URL must be redacted before any budget cut`; removing the count flag reproduces `a count-limited link list must set truncated`.

0.3.28 fixes two consequences of that change. Keeping a URL whole let it subtract from `remaining` without a floor, so a later text field saw a negative budget and `slice(0, negative)` returned text from the *end* of the string — the exact opposite of the intended cut; an over-budget URL is now dropped whole instead of spending the budget, and the text clamp is floored at zero. Separately, the link-count check ran before the `http(s)` eligibility filter, so a page whose last `http` link exactly filled `limit` and then offered only `mailto:`/`tel:` links was reported as truncated; eligibility is now tested first. Coverage: an extract ordering a long label before an over-budget URL and a text field after it (the field must stay within the remaining budget), and a `links.test` page read at `limit:4` with trailing non-http links (must not report truncation). Reverting either change fails those assertions.

0.3.29 keeps extract URL values inside the declared per-value limit. After the URL was made to travel whole, its only remaining bounds were the 8192 hard cap and the total budget, so a 4001–6000 character `href` was returned even though `ghost.json` declares each field value at most 4000 characters. A URL value is now dropped whole when it exceeds 4000 (or the remaining budget) and the result reports `truncated:true`, so the declared limit holds without ever cutting a URL before redaction. Coverage: a fixture link with a 4100-character URL must come back as `null` with `truncated:true`; restoring the 8192-only cap reproduces `a URL over the declared 4000-character value limit is dropped`.

0.3.30 enforces that limit on the redacted string. The extension checked length before the bridge redacted, but masking a short credential lengthens the value (`code=x` becomes `code=REDACTED`), so a URL just under 4000 could be returned overflowing the declared limit. The result exit now re-checks the limit after redaction and drops an over-long value whole, marking the result truncated. Coverage: a fixture URL padded to just under the limit carrying `code=x` must come back as `null` with `truncated:true`; removing the post-redaction check reproduces `a URL that grows past the limit during redaction is dropped`.

0.3.31 widens credential matching instead of listing key names, and bounds the page URL. Query keys are now judged per word, so composite names such as `reset_token` or `magic_link_token` are masked even when the value is short, and a parameter whose value is itself a URL or path is analysed recursively (`?next=%2Freset%2F<token>`). Separately, a redirect or `history.replaceState` can stretch the page URL far past the bridge's request limit, which used to make the result unpostable: the URL sent for authorization, the URL handed to the injected function, and the URL in the result are each bounded, and the injected navigation check compares the bounded prefix rather than the whole string. Coverage: unit assertions for the composite keys, the nested URL and an untouched `?page=2&sort=name`; a `pageurl.test` page that rewrites itself to a 600000-character path must read successfully with a URL within 8192 characters. Reverting the caps reproduces `BROWSER_TIMEOUT` on that read.

0.3.32 restores exact page identity. Bounding the URL handed to the injected function had been done by comparing a prefix, which also accepted a same-document navigation to a URL sharing that prefix; the injected code now fetches the exact target URL from the worker over a message and requires `location.href` to match it fully. The URL is deliberately not passed as an `executeScript` argument (that never settled for a very long URL) nor read from a capped copy. Coverage: the whole existing Chromium suite runs through this path, including the `pageurl.test` read whose URL is 600000 characters, and the redirect-denial case that depends on the identity check.

0.3.33 makes sensitivity follow the editing host. Naming was read from the element itself, so a child span of `<div contenteditable name="otp">` was judged ordinary while the host was protected: `extract` could return the code and `type` could accept the child. Sensitivity now resolves the nearest `[contenteditable]` ancestor-or-self and applies the naming rule to that host, so reading, snapshots and interactions all inherit the boundary. Coverage: the fixture's OTP value now sits inside a nested span, which must neither be readable through `extract` nor accept `type`; restoring own-attribute-only detection fails those assertions.

0.3.34 keeps that boundary across nesting. Resolving only the nearest `[contenteditable]` let an inner unnamed region, or a `contenteditable="false"` node, mask an outer host named like a credential, so its descendants were readable and typable again. Sensitivity now walks the entire ancestor chain and treats the element as sensitive when any ancestor-or-self is an editable host with a credential name (the unused `isField` helper is gone). Coverage: the fixture's OTP host now contains both a `contenteditable="false"` span and an unnamed inner editable region holding values, neither of which may be read through `extract` or accept `type`; restoring the nearest-host lookup fails those assertions.

0.3.35 applies the same inheritance rule to the text projections and corrects the installation wording. The root guard still required the root itself to match `TEXT_SCOPE`, so `mode:"text"` with `selector` on a plain span inside a sensitive editing host returned the credential through the exact-`innerText` path; the guard now uses the shared sensitivity rule, so every projection inherits the same boundary. On installation: the previous note promised "no unzip or directory selection needed", but a dropped ZIP is accepted only where the build and policy allow it and that path is not verified on every build, so the note, the `browser_status` instructions and the Manual now say a refusal is expected and point at Load unpacked on the bundled directory, which also needs no manual unzip. Coverage: a text-mode read of the nested span must not return the value; reverting the guard fails that assertion.

0.3.36 stops the link scan at the requested count. The loop materialised and visibility-filtered every anchor first, so a page with tens of thousands of links paid a full layout sweep even for `limit:1`; eligibility, visibility and the count are now checked in that order per candidate and the loop exits as soon as the limit is met. Coverage: a 20000-link fixture page read at `limit:1` must return exactly one link and report truncation. This is a structural change — at that size the previous scan is not slow enough for a timing threshold to be discriminating, so none is asserted.

A packaged ZIP can also be dragged onto Chromium's extensions manager as a developer-install alternative to selecting a directory. Browser developer-mode/policy requirements still apply; this is not a store-signed release or an automatic-update promise. Chromium's implementation supports this path; this machine's official Chrome ZIP-drop flow has not yet been verified. A self-packed CRX may be blocked rather than merely showing a dismissible warning.

Provisioning retains an empty staged audience. No marketplace admission, store submission, push, PR or public release is implied. The four HEAD-based package-contract tests also passed against the committed snapshot containing the new plugin and provisioning entry.

Review regressions: public hostname served by a real loopback fixture returns no DOM/title; positive fixture IPs are simulated at the browser API boundary in the isolated Chromium suite. Navigation/reload identity, late responses and unknown address paths are covered by unit tests.

## Runtime source evidence

`node/worker.cjs` is the reviewed first-party source entry, shipped byte-for-byte without transpilation or a separate generated/vendor copy. Its dependency graph is `worker.cjs` → `node:readline`, `node/bridge.cjs`, `node/installation.cjs`, `extension/policy.js`; these require only Node built-ins and the bundled manifest/distribution JSON. There is no external package name/version or upstream binary to reconstruct. `.github/scripts/package-plugin.sh my-browser <output.cindy>` packages the committed source; compare the extracted entry with `git show HEAD:my-browser/node/worker.cjs` to verify provenance.

Network/launch inventory: the worker creates only a loopback HTTP listener on 18810–18819; the extension polls those exact ports. Website navigation is HTTP(S) for the requested browser task. Installation opens fixed extension-manager URLs, bundled files, or configured store URLs validated against `chromewebstore.google.com`, `microsoftedge.microsoft.com`, `apps.apple.com`; the current store URLs are unset. Installation/signature checks use fixed executables and argv, not shell commands. Source inspection found no eval/Function/string-code execution or Math.random; the only base64 runtime decode is the public extension identity key. Loopback contract approval and bridge authentication remain unresolved review items; this evidence does not waive them.

Status tool output explicitly selects connection and installation fields; local paths, policy and pairing-origin details stay in the settings response. Extract attributes are limited to href/src/datetime/title/alt/aria-label/role before dispatch. Arbitrary token attributes are rejected, but site text and links can still contain private information.

Read failures after starting navigation report `execution: unknown`: the website may already have received the visit. A lost acknowledged read also reports unknown; jobs that expire before acknowledgement remain not executed.

0.3.37 applies the extraction character budget at the Node result exit after URL redaction, shared across all fields and records. Over-budget URLs are omitted whole; ordinary text uses the remaining budget. Truncation is reported. HTTP regression tests cover single/multiple records, default/custom budgets and caller-defined field names.

0.3.38 invalidates affected acknowledged jobs on revocation (unknown outcome), including their authorized redirect destination, and rechecks authorization from the injected document before returning its target. A DOM action already past that final check may still finish; revocation cannot roll back an executed action.

0.3.39 checks both original and redirected URLs on revocation. Once the injected document has obtained its final dispatch permission, saving a revocation waits for completion and discards the result. A disconnected or timed-out dispatch returns an unconfirmed outcome; repeated saves cannot turn that uncertainty into success. Already executed effects cannot be rolled back.

0.3.40 separates uncertain dispatch history from the 16 live-job slots. At most 128 compact records retain authorization metadata, not interaction payloads. Overflow keeps a conservative uncertainty flag. Applied permissions permit fresh authorized work even while an earlier action remains unconfirmed; saving permissions still reports uncertainty.

0.3.41 distinguishes applied permissions from uncertain earlier actions: saving an applied policy succeeds with an explicit warning, updates the settings baseline and clears unsaved changes. It does not claim the earlier action completed. Text, content, snapshot and link projections share one sensitive-subtree walk, so a named credential descendant inside an unnamed editing host is not returned via innerText.

0.3.42 lets `remove` and the settings chips delete a saved `interact.block` exclusion. Upgrade leftovers could not be cleared before: `P.check` still denied the site, and the settings page only rendered the list as text. Removing an exclusion still requires confirmation.

0.3.43 redacts a credential that shares a path segment with a reset/verify context word (`/verify-<token>`, `/reset-token-<id>`). Ordinary context-only segments such as `/reset-password/success` stay intact. A KV save that the worker does not confirm still updates the settings baseline so the next save is not blocked by `POLICY_CHANGED`.

0.3.44 keeps descriptive same-segment paths. A remainder sharing a context word is masked only when it is long and contains a digit, so `/login-account-settings-tab-details` stays identifiable while UUID/reset tokens are still redacted.
