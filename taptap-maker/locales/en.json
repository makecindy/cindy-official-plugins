{
  "name": "TapTap Maker",
  "description": "Use the bundled official TapTap Maker Runtime to connect an account, sync or initialize projects, build games, and open official previews. No separate CLI installation or MCP configuration is required.",
  "whenToUse": "Use this plugin for TapTap Maker work in the current Cindy workspace. If the project is elsewhere, ask the user to open it in Cindy; never use Shell, CLI, npx, direct MCP, or a browser. Only “MCP error -32600: INSUFFICIENT_BALANCE” means insufficient credits; offer Art after confirmation.",
  "tools": {
    "maker_status": {
      "description": "Inspect only the current session workspace's TapTap Maker status, including login, project binding, Git, Python, Lua LSP, and tool readiness. If the project is elsewhere, ask the user to open it in Cindy; do not bypass the plugin with Shell, CLI, npx, or direct MCP.",
      "parameters": {
        "/properties/skip_remote_sync": {
          "description": "In full diagnostic mode, set true to skip remote Git sync and development-tool freshness checks. Summary mode is local-only by default."
        },
        "/properties/detail": {
          "description": "Set true only for a full diagnostic; the default is a fast, local, read-only status summary."
        }
      }
    },
    "maker_ads_guide": {
      "description": "Read the bundled official Runtime's TapTap Maker ads integration guide. For ads, rewarded videos, ad placements, or ShowRewardVideoAd, call this before checking project status, calling get_ad_config, or changing ad code. This tool does not modify the project."
    },
    "maker_login": {
      "description": "Open browser authorization through the official TapTap Maker CLI and connect an account. An existing PAT can also be configured manually in the plugin settings."
    },
    "maker_apps": {
      "description": "List projects available to the current TapTap Maker account so an app_id can be selected before initialization."
    },
    "maker_init": {
      "description": "Initialize only the current session workspace as a TapTap Maker project. If the project is elsewhere, ask the user to open it in Cindy; do not bypass the plugin with Shell, CLI, npx, or direct MCP. Pass app_id for an existing project; pass create=true and name only when the user explicitly asks to create one.",
      "parameters": {
        "/properties/app_id": {
          "description": "The app id of an existing TapTap Maker project; mutually exclusive with create/name."
        },
        "/properties/create": {
          "description": "Pass true only when the user explicitly asks to create a new project."
        },
        "/properties/name": {
          "description": "The new project name; required when create=true."
        }
      }
    },
    "maker_doctor": {
      "description": "Diagnose the TapTap Maker local development environment and project configuration in the current session work directory."
    },
    "maker_build": {
      "description": "Use the bundled official Runtime to sync, submit, and build the current session workspace only when explicitly requested. Open successful previews in Cindy's right-side browser and quote user_facing_markdown verbatim. Do not use the system browser, an external CLI, direct MCP, or the internal build tool.",
      "parameters": {
        "/properties/entry": {
          "description": "Optional single-player Lua entry file, relative to scriptsPath."
        },
        "/properties/scriptsPath": {
          "description": "Optional scripts directory, relative to the project root."
        },
        "/properties/entry_client": {
          "description": "Optional multiplayer client Lua entry file."
        },
        "/properties/entry_server": {
          "description": "Optional multiplayer server Lua entry file."
        },
        "/properties/multiplayer": {
          "description": "Optional official Maker multiplayer configuration."
        },
        "/properties/message": {
          "description": "Optional commit message for this Maker submission."
        },
        "/properties/files": {
          "description": "Optional project-relative file paths to submit."
        },
        "/properties/confirm_remote_build_without_submit": {
          "description": "Pass true only when the user explicitly asks to leave local changes unsubmitted and build the remote version."
        }
      }
    },
    "maker_list_tools": {
      "description": "List the official Maker dynamic tools and parameter schemas currently available in the workspace. Always call this before maker_call_tool. If the project is elsewhere, ask the user to switch workspaces; do not bypass the plugin with Shell, CLI, npx, or direct MCP."
    },
    "maker_call_tool": {
      "description": "Call an official Maker dynamic tool just returned by maker_list_tools in the current workspace; never bypass the plugin with Shell, CLI, npx, or direct MCP. If .project/project.json is missing, explain that building submits and pushes the project, then call maker_build only after confirmation and retry. The plugin handles missing_taptap_identity once. Only the exact error “MCP error -32600: INSUFFICIENT_BALANCE” means insufficient credits; offer Art only after confirmation.",
      "parameters": {
        "/properties/name": {
          "description": "The dynamic tool name from the most recent maker_list_tools result."
        },
        "/properties/args": {
          "description": "The dynamic tool's argument object; pass {} when it has no arguments."
        }
      }
    }
  }
}
